# The App Dashboard

## Windows Shortcut

To launch the dashboard on Windows, double-click the `LaunchDashboard.bat` file in the project root. The script opens `Dashboard\index.html` in your default browser. If the HTML file is moved or renamed, update the path in the batch file accordingly.
